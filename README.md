# Usage
Clone this repository in `~/.config/sh-config` and add the following line to your `~/.bashrc` or `~/.zshrc` when possible to the top of the file:
``` sh
source ~/.config/sh-config/shrc.sh
```
